# Build resources

electron-builder auto-detects app icons placed in this directory. Drop in any of:

- `icon.icns` — macOS
- `icon.ico` — Windows
- `icon.png` — Linux (512×512 recommended)

Without these, packaging falls back to the default Electron icon.
